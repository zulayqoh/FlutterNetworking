



import 'package:flutter/material.dart';

void main() => runApp(FlutterNetworking());

class FlutterNetworking extends StatelessWidget {
  const FlutterNetworking({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

